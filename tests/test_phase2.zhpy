# Phase 2 测试：控制流增强

# 一、链式条件
输出 "测试链式条件"

将score定义为85

如果 score 大于等于 90 那么
    输出 "优秀"
否则如果 score 大于等于 80 那么
    输出 "良好"
否则如果 score 大于等于 60 那么
    输出 "及格"
否则
    输出 "不及格"

# 二、当循环
输出 "测试当循环"

将i定义为0
当 i 小于 5 时循环
    输出 变量i
    i 加等于 1

# 三、对于循环
输出 "测试对于循环"

将fruits定义为["苹果", "香蕉", "橙子"]
对于 fruit 在 fruits 中的每一个
    输出 变量fruit

# 四、重复循环
输出 "测试重复循环"

重复 3 次
    输出 "重复中"

# 五、跳出与继续
输出 "测试跳出继续"

将j定义为0
当 j 小于 10 时循环
    j 加等于 1
    如果 j 等于 3 那么
        继续
    如果 j 等于 6 那么
        跳出
    输出 变量j

# 六、异常处理
输出 "测试异常处理"

尝试
    将result定义为10 除以 0
    输出 变量result
捕获 当错误为 ZeroDivisionError
    输出 "捕获到除零错误"
最终
    输出 "最终块执行"

# 七、函数定义与调用
输出 "测试函数"

定义函数  greet 参数为 name
    输出 "你好，"
    输出 变量name

greet("世界")

定义函数  add 参数为 a, b
    返回 a 加 b

将sum定义为add(3, 5)
输出 变量sum

# 八、类定义
输出 "测试类"

定义类  Person
    定义函数 __init__ 参数为 self, name
        自身.name = name

    定义函数 hello 参数为 self
        输出 "你好"

将p定义为Person("小明")
p.hello()
输出 变量p.name
