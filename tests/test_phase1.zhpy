# Phase 1 测试：数据类型 + 全部运算符

# 一、基础数据类型
输出 "测试基础数据类型"

# 整数
将a定义为10
输出 变量a

# 浮点数
将b定义为3.14
输出 变量b

# 字符串
将c定义为"你好世界"
输出 变量c

# 布尔值
将d定义为真
输出 变量d

将e定义为假
输出 变量e

# 空值
将f定义为空
输出 变量f

# 二、算术运算符
输出 "测试算术运算"

将x定义为10
将y定义为3

令 加法 为 x 加 y
输出 变量加法

令 减法 为 x 减 y
输出 变量减法

令 乘法 为 x 乘 y
输出 变量乘法

令 除法 为 x 除以 y
输出 变量除法

令 整除 为 x 整除 y
输出 变量整除

令 取余 为 x 取余 y
输出 变量取余

令 幂 为 x 的 2 次方
输出 变量幂

# 三、比较运算符
输出 "测试比较运算"

将p定义为5
将q定义为5
将r定义为10

如果 p 等于 q 那么
    输出 "p等于q"
否则
    输出 "p不等于q"

如果 p 不等于 r 那么
    输出 "p不等于r"

如果 p 大于 3 那么
    输出 "p大于3"

如果 p 小于 r 那么
    输出 "p小于r"

如果 p 大于等于 5 那么
    输出 "p大于等于5"

如果 p 小于等于 5 那么
    输出 "p小于等于5"

# 四、逻辑运算符
输出 "测试逻辑运算"

将m定义为真
将n定义为假

如果 m 与 真 那么
    输出 "m与真为真"

如果 n 或 真 那么
    输出 "n或真为真"

如果 非 n 那么
    输出 "非n为真"

# 五、位运算符
输出 "测试位运算"

将bit1定义为5
将bit2定义为3

令 位与 为 bit1 位与 bit2
输出 变量位与

令 位或 为 bit1 位或 bit2
输出 变量位或

令 位异或 为 bit1 位异或 bit2
输出 变量位异或

令 左移 为 bit1 左移 1
输出 变量左移

令 右移 为 bit1 右移 1
输出 变量右移

# 六、赋值运算符
输出 "测试赋值运算"

将val定义为10
输出 变量val

val 加等于 5
输出 "val加等于5后"
输出 变量val

val 减等于 3
输出 "val减等于3后"
输出 变量val

val 乘等于 2
输出 "val乘等于2后"
输出 变量val

val 除等于 2
输出 "val除等于2后"
输出 变量val

# 七、成员运算符
输出 "测试成员运算"

将my_list定义为[1, 2, 3, 4, 5]

如果 3 属于 my_list 那么
    输出 "3在列表中"

如果 10 不属于 my_list 那么
    输出 "10不在列表中"

# 八、身份运算符
输出 "测试身份运算"

将obj1定义为空
将obj2定义为obj1

如果 obj2 是 obj1 那么
    输出 "obj2就是obj1"

如果 obj2 不是 空 那么
    输出 "obj2不是空"
